# MonkAi Business — CLAUDE.md

Durable knowledge base for future sessions on this repo. Keep this file accurate: update it whenever facts here change, and append to **Lessons learned** after every session that touches this codebase.

## Project

MonkAi Business is the AI-offering of **SDK Solutions BV** — a marketing site for AI-adoption coaching aimed at Flemish SMEs ("KMO's"), delivered by **Stijn De Ketelaere**. It is a static **Astro 5** site, content and copy in **Dutch (nl)**, deployed on **Netlify**.

The site is a single long-scroll homepage plus three sub-areas: a blog, a use-cases library, and a Netlify-Forms-powered contact flow with a thank-you page. The homepage sections, in their actual assembly order in `src/pages/index.astro`, are: Nav, Hero, Problem, Statement, Ladder, Approach, UseCases, Services, BeyondChat, AiAct ("EU AI Act" note), Agreement ("Onze afspraak"), BlogTeaser, About ("Over Stijn"), Contact, Footer.

## Writing style (IMPORTANT)

Avoid "AI slop" in all copy: site content, commit messages, docs, and chat replies.

- **Never use an em dash (—) or en dash (–). Use a plain hyphen "-" instead.** This is a hard rule. Where an em dash would join clauses, either use a hyphen with spaces (" - ") or rewrite as two sentences.
- Write plainly and concretely. Avoid filler, hype, and the typical LLM tics (e.g. "in het huidige landschap", "het is belangrijk om op te merken", overuse of "bovendien"/"daarnaast", tidy rule-of-three lists everywhere).
- Match the existing Dutch tone: direct, nuchter, no marketing bloat.

Note: older text in this file and in the codebase still contains em dashes from before this rule. Do not introduce new ones; replace them with "-" when you touch a file anyway.

## Verify facts before you publish (IMPORTANT)

Any factual claim that goes into public copy - blog posts, use cases, homepage text, anything a reader could act on - must be verified before it ships, not written from memory. This is a hard rule.

- Especially verify claims about **third-party products, AI models, laws, dates, prices and numbers** (e.g. "Copilot uses model X", "the EU AI Act requires Y", "tool Z costs €N"). Products and models change fast; the knowledge cutoff is not a source. Use web search and prefer primary sources (the vendor's own docs/blog, official legal texts).
- Correct the brief when the facts disagree with it. A user's framing can be loosely true but misleading for our audience - Flemish KMOs are EU tenants, so EU-specific caveats (data residency, opt-in defaults, GDPR) matter and must be stated, not glossed over. Being accurate and nuanced beats being impressive.
- Do not invent statistics or ROI percentages to sound convincing. If you can't verify a number, argue qualitatively with concrete examples instead.

## Design provenance

The visual design originates from a **Claude Design** project, not from this repo directly:

- Project name: **"Drie style tiles klaar"**
- Project ID: `d12a143b-8fe1-42af-bd49-3ebc7160a3cf`
- Files in that project:
  - `Homepage.dc.html` — the homepage design that was ported into this site.
  - `Style Tiles.dc.html` — the design-system reference (colors, type, components).
  - `favicon.svg`

**Important — what a `.dc.html` file actually is:** these are Claude **design canvases**, not deployable websites. Each one contains a **desktop variant** (`<div id="4a">`, 1440px wide) and a **mobile variant** (`<div id="4b">`, 390px wide) laid out side by side, wrapped in `<x-dc>` / `<helmet>` markup with Claude's own canvas tooling (`support.js`, `image-slot.js`, `<image-slot>` custom elements). None of that wrapper or tooling is ported — it exists only to let the canvas render inside Claude's design surface.

**Porting rule:** the **desktop `id="4a"`** frame is the source of truth for markup/content/layout at desktop width; the **mobile `id="4b"`** frame is the responsive reference used to size the same component down to small screens (breakpoints, stacking, font-size drops). When the two disagree on content, `4a` wins and `4b` is treated as "how this looks small," not as a second source of truth.

A local snapshot of the ported homepage source is kept at `docs/design-source/Homepage.dc.html` for reference — it is not built or imported by the app.

**How to re-pull / update the design later:** use the `claude_design` MCP server, tool **`DesignSync`**:
1. `list_files` (method, with the `projectId` above) to see what's currently in the project.
2. `get_file` (method, with the same `projectId` and a file name) to fetch the latest contents of `Homepage.dc.html`, `Style Tiles.dc.html`, or `favicon.svg`.
3. Diff the fetched `id="4a"` markup against what's currently in `src/components/`, then re-port only the changed sections into the matching Astro component(s) — translating inline styles to the tokens in `src/styles/tokens.css` as you go (see below).

## Design tokens

Canonical tokens live in **`src/styles/tokens.css`**, loaded globally. **Never hardcode a hex color, radius, or font stack in a component where a token already exists for it** — add a new token instead if the design introduces a genuinely new value.

| Token | Value | Use |
|---|---|---|
| `--bg-page` | `#F4F2EC` | Page background |
| `--bg-canvas` | `#E7E5DE` | Canvas/section background |
| `--ink` | `#24261F` | Primary text |
| `--ink-soft` | `#4A4E42` | Secondary text |
| `--muted` | `#656A5C` | Muted/tertiary text |
| `--border` | `#D9DACE` | Default border color |
| `--card` | `#FBFAF6` | Card background (light) |
| `--card-alt` | `#E9EADF` | Alternate card background |
| `--section-alt` | `#EFEDE5` | Alternate section background |
| `--green` | `#4C5F3B` | Brand green (buttons, links, accents) |
| `--green-hover` | `#37472A` | Brand green, hover/active state |
| `--green-light` | `#8FA478` | Light green accent |
| `--dark` | `#24261F` | Dark surface background (e.g. dark cards) |
| `--dark-text` | `#C9CBBF` | Text on dark surfaces |
| `--dark-dim` | `#A9AC9C` | Dimmed text on dark surfaces |
| `--input-bg` | `#FFFFFF` | Form input background |
| `--placeholder` | `#A5A79C` | Form placeholder text color |
| `--maxw` | `1120px` | Content max-width |
| `--radius-sm` | `4px` | Small corner radius (inputs, chips) |
| `--radius` | `6px` | Default corner radius (cards, buttons) |
| `--font-serif` | `'Source Serif 4', serif` | Headings |
| `--font-sans` | `'Work Sans', sans-serif` | Body/UI text |
| `--font-mono` | `'JetBrains Mono', monospace` | Mono accents (labels, tags) |

## Structure

```
src/
  layouts/
    BaseLayout.astro       — shared <head>, fonts, global.css/tokens.css import, Nav + Footer slot wrapper
  components/
    Nav.astro, Footer.astro, Logo.astro       — global chrome, used on every page
    Hero.astro, Problem.astro, Approach.astro,
    Ladder.astro, Agreement.astro, Services.astro,
    BeyondChat.astro, AiAct.astro, About.astro,
    Statement.astro                            — static homepage sections, ported 1:1 from Homepage.dc.html (4a)
    UseCases.astro, BlogTeaser.astro           — homepage sections driven by content collections
    Contact.astro                              — the Netlify-Forms contact form section
    VideoEmbed.astro                           — renders a use case's video (file/YouTube/Vimeo variants)
  content/
    blog/*.md              — blog post collection (see "Add a blog post" below)
    usecases/*.md           — use case collection (see "Add a use case" below)
    resources/*.md          — download/prompt collection for the hidden /data page (see "The /data page" below)
    inspiratie/*.md         — kopieerbaar materiaal voor de verborgen /inspiratie-pagina (zie onder)
  content.config.ts        — Astro 5 collection definitions (glob loader + zod schemas) for blog, usecases, resources & inspiratie
  pages/
    index.astro             — homepage, assembles all sections in order
    data/index.astro         — hidden /data besloten zone (client-side login + tool tabs, see below)
    inspiratie/index.astro   — verborgen /inspiratie-pagina voor sessiedeelnemers (systeemtabs + kopieerknoppen, zie onder)
    bedankt.astro            — thank-you page the contact form POSTs/redirects to
    blog/index.astro         — blog listing (single column + zoekveld + tagfilter, see below)
    blog/[slug].astro        — blog post detail (dynamic route, slug = entry.id)
    use-cases/index.astro    — use case listing
    use-cases/[slug].astro   — use case detail (dynamic route, slug = entry.id)
  styles/
    tokens.css               — design tokens (see above), the single source of truth for colors/fonts/shape
    global.css                — resets, base element styles, typography defaults
public/
  favicon.svg
  media/
    stijn.svg               — placeholder profile image (see Deploy section)
docs/
  design-source/Homepage.dc.html   — local snapshot of the ported design canvas, for reference only
  superpowers/                     — spec/plan docs from the build process (not app code)
.superpowers/sdd/                  — per-task briefs/reports/diffs from this project's staged build (task-1..8)
netlify.toml                       — Netlify build config (see Deploy)
CLAUDE.md                          — this file
```

## How to add a blog post

Create `src/content/blog/<slug>.md` with frontmatter:

```yaml
---
title: "Post title"
date: 2026-07-23
description: "One or two sentence summary shown in listings and previews."
tags: ["governance", "ai-adoptie"]
image: "/media/blog/<slug>.svg"
imageAlt: "Korte beschrijving van het beeld"
draft: false
---
Post body in Markdown.
```

- `title` (string, required)
- `date` (string, required, `YYYY-MM-DD`)
- `description` (string, required)
- `tags` (array of strings, optional) — **kies uit de vaste woordenlijst**, altijd kleine letters. De set (10 tags, door Stijn vastgelegd) is:
  - **thema's:** `ai-adoptie`, `governance` (inclusief wetgeving/AI Act), `productiviteit`, `kennis`, `agents`, `automatisatie`
  - **tools:** `claude`, `chatgpt`, `copilot`, `gemini`
  Reken op 2 tot 3 tags voor een themapost. Een tooltag zet je erbij wanneer de post echt iets vertelt wat je in dát product doet, niet bij een terloopse vermelding - een vergelijkende post mag daardoor gerust 5 of 6 tags hebben. Elke tag is een filterknop op `/blog`, dus een nieuwe tag toevoegen betekent een extra knop: doe dat alleen als het onderwerp echt niet in de set past, en gebruik hem dan meteen op minstens twee posts. Verkeerde schrijfwijzen (`Claude`, `adoptie`, `kmo`, `wetgeving`) geven een aparte, dubbele knop. Tags met een eigen schrijfwijze staan in de `labels`-map bovenaan `blog/index.astro` (`ai-adoptie` → "AI-adoptie", `chatgpt` → "ChatGPT"); de rest krijgt automatisch een hoofdletter. **Voeg een nieuwe merknaam daar toe**, anders komt hij als "Chatgpt"-achtig label in beeld.
- `image` (string, optional) — cover/thumbnail path. Renders as a 3:2 cover on the detail page and as the card thumbnail in `/blog` + the homepage teaser; also fed into the `BlogPosting` schema. Posts without it still build (no image shown). The four existing posts use on-brand SVG covers in `public/media/blog/` (hand-authored, brand hex hardcoded because an `<img>`-loaded SVG can't read `tokens.css` variables).
- `imageAlt` (string, optional) — alt text for `image`
- `draft` (boolean, optional, default `false`)

The homepage teaser section automatically shows the **newest 3 non-draft** posts (sorted by `date`, descending) in a 3-koloms grid. `/blog` lists all non-draft posts als **één kolom onder elkaar** (thumbnail links, tekst rechts; op mobiel gestapeld) met bovenaan een **zoekveld en tagfilters**. Beide zijn client-side: het filteren gebeurt in een inline script op `blog/index.astro`, de tagknoppen worden bij het builden afgeleid uit de frontmatter (gesorteerd op aantal posts). Meerdere tags aanklikken = OF-logica; de zoekterm werkt daar bovenop (EN) en zoekt in titel, description en tags, niet in de bodytekst. De actieve filters staan in de querystring (`/blog?tags=governance,kennis&q=copilot`), dus zo'n link is deelbaar en wordt bij het laden weer toegepast. Each post's detail page is served at `/blog/<slug>` (the file's slug, taken from `entry.id`).

## How to add a use case

Create `src/content/usecases/<slug>.md` with frontmatter:

```yaml
---
title: "Use case title"
order: 10
summary: "One or two sentence summary shown in the listing card."
video:
  type: file        # or: youtube | vimeo
  src: /media/example.mp4
  poster: /media/example-poster.jpg
draft: false
---
Use case body in Markdown.
```

- `title` (string, required)
- `order` (number, required) — controls sort order in the listing (ascending)
- `summary` (string, required)
- `video` (optional object): `type` is `file`, `youtube`, or `vimeo`; `src` is the file path or embed URL; `poster` (optional) is a poster image path, used with `type: file`
- `draft` (boolean, optional, default `false`)

For a local video, drop the `.mp4` (and poster image, if any) into `public/media/` and reference them as `/media/<filename>` with `type: file`. For a hosted video, use the YouTube or Vimeo embed URL with `type: youtube` or `type: vimeo` — `VideoEmbed.astro` renders the right markup for each case. The detail page is served at `/use-cases/<slug>`.

## The `/data` page (besloten zone) — how to add a resource

`/data` (`src/pages/data/index.astro`) is a **hidden, `noindex`, not-linked** page with a **client-side login** (`monkai` / `business`, checked in an inline script, `sessionStorage` flag `monkai_data_unlock`). After unlocking, five tool tabs (Claude / ChatGPT / Copilot / Gemini / Overig) filter downloadable resources.

⚠️ **This is obscurity, not security.** The password sits in the page source and every file under `public/data/files/` is reachable by direct URL regardless of login. Only ever put non-sensitive material here (workshop hand-outs, prompt templates). This was a deliberate choice (see lesson #11); do **not** describe it to the user as protected.

To add a resource, create `src/content/resources/<slug>.md` (body can be empty — only the frontmatter is used):

```yaml
---
title: "Offerte-prompt voor Claude"
tool: "claude"          # claude | chatgpt | copilot | gemini | overig
description: "Korte uitleg wat het is."
file: "/data/files/offerte-prompt.pptx"
type: "pptx"            # free-form label shown as a chip: pptx | pdf | prompt | docx | zip ...
order: 10               # ascending sort within a tool
draft: false
---
```

Drop the actual download (PowerPoint/PDF/prompt/...) into `public/data/files/` and point `file` at it. Empty tools render "Nog geen content voor <tool>." The `resources` collection is defined in `src/content.config.ts`; `/data` is filtered out of the sitemap in `astro.config.mjs`.

## De `/inspiratie` pagina - materiaal voor sessiedeelnemers

`/inspiratie` (`src/pages/inspiratie/index.astro`) is **verborgen**: `noindex`, uit de sitemap gefilterd, nergens gelinkt, en **zonder** inlogdrempel (deelnemers moeten meteen kunnen ophalen). Zelfde regel als bij `/data`: wie de URL heeft ziet alles, dus **niets gevoeligs** erop.

De pagina heeft **systeemtabs** (Claude / ChatGPT / Copilot / Gemini / Overal) en groepeert per tab op **categorie**: `instructies`, `agents` ("Expert-collega's"), `prompts`, `skills`, `naslag`. Elk item toont titel + omschrijving, een **Kopieer tekst**-knop (clipboard, met `execCommand`-fallback) en de tekst in een `<details>` met `<pre>`. De tab zit in de querystring (`/inspiratie?systeem=gemini`), dus zo'n link is deelbaar.

Nieuw item = `src/content/inspiratie/<slug>.md`:

```yaml
---
title: "SEO-expert"
system: ["claude", "chatgpt", "copilot", "gemini"]   # één string of een lijst
category: "agents"        # instructies | agents | prompts | skills | naslag
description: "Wat het is en waarvoor je het gebruikt."
file: "/data/files/iets.pptx"    # optioneel: dan een downloadknop i.p.v. kopieerknop
documents:                        # optioneel: bronbestanden die bij dit item horen
  - title: "Functieomschrijvingen 2026"
    file: "/inspiratie/files/functieomschrijvingen.pdf"
    type: "pdf"
    note: "optionele korte nota"
order: 10
draft: false
---
De letterlijke tekst die de klant kopieert.
```

`documents` rendert een blok "Hang deze documenten erbij als bron" onder de omschrijving, met een downloadlink per document. Gebruik dat voor een expert-collega die zonder bronnen weinig kan (de HR-expert bijvoorbeeld): de klant kopieert de instructie én hangt die bestanden aan zijn project, GPT, Gem of agent. De bestanden zelf staan in **`public/inspiratie/files/`**. Wijs nooit naar een bestand dat er nog niet staat: dat geeft een dode downloadlink op een live pagina.

**Belangrijk:** de **body wordt niet als markdown gerenderd** maar onbewerkt via `entry.body` in een `<pre>` gezet - dat is precies de bedoeling, want de klant moet de `## koppen` en `**vet**` mee kunnen kopiëren naar zijn instructieveld. Gebruik dus geen markdown-syntax die je gerenderd wil zien, en `render()` is hier niet nodig. `system` mag een lijst zijn: hetzelfde bestand verschijnt dan onder meerdere tabs (één bron van waarheid voor een expert-collega die overal hetzelfde werkt). Lege categorieën renderen niet; een tab zonder materiaal toont "Nog geen materiaal voor <systeem>". Per systeem staat er bovenaan één `note`-regel met waar je zulke tekst in dat systeem zet - die placement-uitleg staat in de pagina, niet in de content.

## Deploy

- GitHub repo: **`MonkAi-Business/monkai.website`** (already set as the `origin` remote).
- **Domain + Netlify: LIVE (connected 2026-07-24).** The user **owns `monkai.business`** and the GitHub repo is **connected to Netlify** (team `sdksolutionsbe`). Netlify reads `netlify.toml` at the repo root (`npm run build`, publish directory `dist`, Node 20) and **redeploys automatically on every push to `main`**. So: pushing to `main` = deploying to production.
- **⚠️ PostHog env var must be set in Netlify:** the analytics key lives in local `.env` only (gitignored). Netlify builds in the cloud and does **not** see `.env`, so `PUBLIC_POSTHOG_KEY` must be added under Netlify → Site settings → Environment variables, or production builds ship with analytics disabled. (Verify whether the user set this.)
- **Alternative — CLI:** `netlify deploy --prod`, authenticated with a Netlify personal access token.
- **Contact form:** uses **Netlify Forms** — the form has `data-netlify="true"`, a hidden `form-name` input matching the form's `name` attribute, and a honeypot field (`netlify-honeypot="bot-field"` + a hidden `bot-field` input). Netlify's build-time HTML parser detects the form from this static markup; no JS or serverless function is needed. Submissions appear under the site's **Forms** tab in the Netlify dashboard after the *first* deploy (the form must exist in a deployed build for Netlify to register it).
- **Analytics (PostHog):** wired via `src/components/Analytics.astro` (rendered in `BaseLayout` head). Set **`PUBLIC_POSTHOG_KEY`** as a Netlify environment variable (Site settings → Environment variables) for production — Astro inlines `PUBLIC_`-prefixed vars at build time, so it must exist in the build environment. Locally, copy `.env.example` to `.env` and fill the key. Without the key, the component renders nothing (no broken script). **Analytics only runs in a production build AND only when `location.hostname` equals the host from `astro.config.mjs` (`monkai.business`)** — so `astro dev` and Netlify deploy previews (`*.netlify.app`) never pollute the numbers. See lessons #9 and #16.
- **Canonical host:** `netlify.toml` 301-redirects `www.monkai.business` → `monkai.business` (both http and https, `force = true`). Keep the apex as the single host; the analytics host guard also only accepts the apex.
- **Contact email:** the public/contact address is **`stijn@monkai.business`** (changed sitewide from `hallo@` on 2026-07-24).
- **Known placeholders awaiting real assets:** the real profile photo is in place (`public/media/stijn.jpg`, 760×880 mozjpeg from the user's portrait, cropped from top; `About.astro` references it). The use-case videos are still placeholders — swap them for real media when available; no code changes should be needed beyond updating file paths in frontmatter. The PostHog key is still needed for analytics to run in production.

## Commands

- `npm run dev` — start the local dev server.
- `npm run build` — build the static site to `dist/`.
- `npm run preview` — preview the production build locally.

## Lessons learned

Running log — append an entry here after any future session that changes this codebase, so the project gets smarter over time.

1. The design lives as a Claude design canvas (`.dc.html`) with a desktop `id="4a"` frame and a mobile `id="4b"` frame side by side; port from `4a`, size mobile behavior from `4b`. The canvas file itself is not directly deployable — it's a design artifact, not a website.
2. Astro 5 content collections are configured in **`src/content.config.ts`** at the project root (NOT the older `src/content/config.ts` convention from Astro 2–4). Use the `glob` loader from `astro/loaders`, import `render()` from `astro:content` to render an entry's Markdown body, and use `entry.id` as the URL slug.
3. Every inline hex value in the design maps to a token CSS variable — keep `tokens.css` authoritative. When the design introduces a new value with no matching token (e.g. `--input-bg`, `--placeholder` for form styling), add a new token rather than hardcoding the hex in a component.
4. Nav/Footer links that point at homepage sections must be root-absolute (`/#aanpak`), not bare fragment links (`#aanpak`) — bare fragments break when the link is rendered on `/blog` or `/use-cases` sub-pages, since they'd resolve relative to the current path instead of the homepage.
5. Netlify Forms needs exactly three things present in the static (build-time) HTML to be detected: `data-netlify="true"` on the `<form>`, a hidden `<input name="form-name" value="...">` matching the form's `name` attribute, and a honeypot field (`netlify-honeypot` attribute + matching hidden input). No JavaScript or serverless function is involved in detection.
6. The design's Services section has **8 regular cards + 1 dark "Raad van advies" card (9 total)** — when porting from a design canvas, verify exact counts/variants against the actual `id="4a"` source markup, not against a prose summary of the section, since summaries can silently drop an outlier card.
7. Responsive behavior: multi-column grids collapse to a single column at `max-width: 768px`. Watch for `flex` rows with `flex-wrap: nowrap` and many inline items (e.g. the hero meta row) — these can overflow horizontally at narrow widths (390px) even when the surrounding grid collapses correctly; test the narrowest supported width explicitly, not just one intermediate breakpoint.
8. **SEO/AI-SEO/GEO layer (added 2026-07):** `BaseLayout.astro` is now the single source of head SEO — it builds a self-referencing `<link rel="canonical">` from `Astro.url`, Open Graph + Twitter tags, and always emits sitewide JSON-LD (`ProfessionalService` `#business` + `Person` `#stijn`, with `areaServed` = Oost-/West-Vlaanderen, Gent, Vlaamse Ardennen and `knowsAbout` = AI-adoptie, Claude-training, second/collective brain). It accepts `noindex` (used on `/bedankt`) and a `schema` prop (array of extra JSON-LD objects). Blog posts pass `BlogPosting` + `BreadcrumbList`; use cases pass `BreadcrumbList`. Target keywords are woven into copy + schema, never stuffed (stuffing costs ~-10% AI visibility). `Faq.astro` (homepage, between About and Contact) renders a `FAQPage` and is the main hook for AI citations. Sitemap via `@astrojs/sitemap` (config filters out `/bedankt`) at `/sitemap-index.xml`; `public/robots.txt` allows AI bots (GPTBot, ClaudeBot, PerplexityBot, Google-Extended, …) and references the sitemap; `public/llms.txt` gives AI systems a site overview. The `og:image` is a real 1200×630 PNG (`public/og-image.png`) rasterized from `public/og-image.svg` with sharp (regenerate: `sharp(svg).resize(1200,630).png()`); the SVG is kept as editable source. Legal pages `/privacy` and `/cookies` exist via a shared `LegalLayout.astro` (BaseLayout + Nav + Footer + prose); footer links and the contact-form privacy link point to `/privacy` and `/cookies` (no more dead `#` anchors). A ready-to-use Google Business Profile setup pack lives at `docs/google-business-profile.md` — Claude can't create the GBP itself (needs the user's Google account + verification), so it's pre-filled for copy-paste. **Still open:** the user must actually create/verify the GBP, and fonts are still loaded from Google Fonts (self-hosting would help LCP + GDPR).
9. **Analytics + copy corrections (2026-07-24).** ⚠️ **Het analytics-deel van deze les is achterhaald door lesson #16** (`persistence: 'memory'` brak sessies en identiteit; de site is niet meer cookieloos en de "geen banner nodig"-conclusie geldt niet meer). De rest van de les (contact-e-mail, regio-framing, OG-image) klopt nog. Origineel: PostHog is wired **cookieless** via `src/components/Analytics.astro` (included in `BaseLayout` head): EU host (`eu.i.posthog.com`), `persistence: 'memory'` (no cookies, no localStorage → **no consent banner needed**), `person_profiles: 'identified_only'`, session recording disabled, `respect_dnt: true`. The component only renders when `PUBLIC_POSTHOG_KEY` is set (env var; `.env.example` documents it), so a keyless build simply omits it — verified: with a dummy key the EU host + `persistence:'memory'` appear in `dist/index.html`; without, `posthog` count is 0. The Netlify env var `PUBLIC_POSTHOG_KEY` must be set for production (Astro inlines `PUBLIC_` vars at build). Primary conversion event **`contact_submitted`** fires via an inline script on `/bedankt` (Netlify form `action="/bedankt"`); other clicks are covered by PostHog autocapture. Tradeoff of `persistence:'memory'` on this MPA: each pageload is a fresh anonymous id, so "unique visitors" ≈ pageviews — that's the price of cookieless/no-consent. `/cookies` and `/privacy` were rewritten to disclose the cookieless analytics (legal basis: **gerechtvaardigd belang**, no consent required because it's cookieless + anonymous + honours DNT). Also this session: contact email changed sitewide `hallo@` → **`stijn@monkai.business`**; region framing broadened from "van Gent tot de Vlaamse Ardennen" to **"heel Oost- en West-Vlaanderen"** in the meta-description default (`BaseLayout`), `llms.txt`, and the OG image (SVG text edited + `og-image.png` re-rasterized with sharp). `areaServed` in the schema still lists both provinces + Gent + Vlaamse Ardennen (kept as extra served places — good for local SEO). Domain is fully `monkai.business` everywhere; site is hosted on monkai.business only.
10. **Writing style + blog covers (2026-07-24):** Added a **hard house-style rule** (see "Writing style" near the top): never use em/en dashes (— –), always a plain `-`; avoid AI-slop tics. All four blog posts were swept for dashes. Blog posts now support optional `image` + `imageAlt` frontmatter: rendered as a **2:1 banner cover** on the detail page and as full-bleed **16:9 card thumbnails** in `/blog` + `BlogTeaser` (cards were restructured: `padding` moved off `.card` onto a `.card-body`, image sits above with `overflow:hidden` on the card). **Gotcha:** the `<img>` has `width`/`height` attributes (for CLS), which map to a fixed CSS `height` and make `aspect-ratio` a no-op — the images render at the attribute height regardless of ratio. Fix is `height: auto` in the CSS alongside `aspect-ratio` + `object-fit: cover`. Motifs sit in the central band of each 3:2 SVG, so the 2:1/16:9 crops don't cut anything important. The four covers are **hand-authored on-brand SVGs** in `public/media/blog/` — chosen because there is no image-generation tool available in-session (the user first wanted AI photos, then opted for SVG). Brand hex is hardcoded inside each SVG on purpose: a `<img src>`-loaded SVG does **not** inherit `tokens.css` CSS variables. `image` also feeds the `BlogPosting` JSON-LD.
11. **`/data` besloten zone (2026-07-24):** Added a hidden resources page at `/data` with a **client-side-only** login (`monkai`/`business`) and five tool tabs filtering a new `resources` content collection (downloads live in `public/data/files/`). The user explicitly accepted that this is **obscurity, not security** (password in source, files reachable by direct URL) — fine for non-sensitive workshop material; never present it as protected. **Gotcha caught in visual QA:** toggling an element's `hidden` attribute does nothing if author CSS sets `display` on it — `.gate{display:flex}` beat the UA `[hidden]{display:none}`, so both the login card and the content showed at once. Fixed with `[hidden]{display:none !important}`. Lesson: when using the `hidden` attribute as a visibility toggle, add that override, and always screenshot JS-driven show/hide states rather than trusting a green build. `/data` is `noindex` + filtered out of the sitemap; not linked anywhere.
12. **Positioning correction - sessies, geen formele opleidingen (2026-07-25):** Stijn levert zelf **inspiratiesessies en sparringsessies in kleine groepen**; voor **concrete/formele opleidingen verwijst hij door** naar partners. Do NOT describe MonkAi as giving "Claude-training/opleidingen" as a course. Copy swept accordingly: the FAQ item "Doe je Claude-training voor bedrijven?" answer reframed (question text kept - it's the search phrase people use, a deliberate AEO hook); the two Services cards renamed `Claude-training voor kenniswerkers/developers` → **`Claude voor kenniswerkers/developers`** (both stress "in kleine groepen"); `AI-geletterdheid`-card and `Approach` step 04 dropped the word "opleiding" (→ "op maat" / "begeleiding"); the sitewide word **"Claude-training" → "Claude-sessies"** in the page `<title>`, `BaseLayout` default meta-description + both schema descriptions + both `knowsAbout` arrays, `llms.txt`, and the OG image (`og-image.svg` text edited, `og-image.png` re-rasterized with sharp per lesson #8). Hero meta row changed from `2 uur | 10 deelnemers | bij jou op locatie` to **`Inspiratiesessie | Sparring in kleine groepen | bij jou op locatie`** (user didn't want a headcount there). **Still carrying the old framing (left untouched, flag when relevant):** the Services `AI-inspiratiesessie` card still reads "Twee uur, tien deelnemers, bij jou op locatie", and `docs/google-business-profile.md` still says "Claude-training voor kenniswerkers/developers" + "Opleiding op maat" - update that GBP pack before the user pastes it into Google.
13. **Custom mail-onderwerp + statische form-definitie (2026-07-25):** Het contactformulier zet nu een custom onderwerp op de Netlify-notificatiemail via een verborgen `subject`-veld (vaste fallback "Nieuwe aanvraag via monkai.business", dynamisch aangevuld met naam + bedrijf via een inline script in `Contact.astro`). **Belangrijke valkuil:** Netlify registreert het formulier en zijn velden uit het statische `public/__forms.html`, niet uit de door Astro gerenderde pagina. Velden die daar niet in staan (zoals `subject`) worden bij inzending **genegeerd** - het veld leek te werken maar het onderwerp bleef op de standaard "Form submission from contact form" staan tot `subject` óók aan `__forms.html` werd toegevoegd. Les: elk nieuw veld in het echte formulier moet gespiegeld worden in `__forms.html`, anders slikt Netlify het stil in. Netlify's UI-onderwerp leeg laten; het HTML-`subject`-veld wint. Wacht altijd op de redeploy voor je test - een inzending vóór/tijdens de deploy gebruikt nog de oude definitie.
14. **Copilot-blogpost + fact-checking regel (2026-07-25):** Nieuwe post `src/content/blog/copilot-data-die-je-al-hebt.md` (+ hand-authored SVG-cover `public/media/blog/copilot-data-die-je-al-hebt.svg`, ijsberg-motief) over de verborgen data waar KMO's al op zitten (mail/Teams/SharePoint) en het rendement van kleine Copilot-acties. **Alle productclaims eerst geverifieerd via web search** (zie de nieuwe "Verify facts before you publish"-regel bovenaan). Geverifieerde stand van zaken Microsoft 365 Copilot per juli 2026: draait standaard op de nieuwste **OpenAI GPT-modellen** (GPT-5.6 is sinds 9 juli 2026 het "preferred" model; dezelfde familie als ChatGPT), met **multi-model orchestration** (per taak het best passende model). **Anthropic Claude** (Sonnet 4 / Opus 4.1) toegevoegd sept 2025 als opt-in in o.a. de Researcher-agent + Copilot Studio + Cowork; sinds ~7 jan 2026 default-on voor de meeste commerciële tenants **behalve EU/EFTA/UK - daar staat Claude standaard UIT** omdat die modellen **buiten de EU Data Boundary** draaien (data verlaat Azure). Guardrails ("enterprise data protection"): prompts/antwoorden/Graph-data worden **niet gebruikt om de foundation-modellen te trainen**, Copilot respecteert bestaande rechten/gevoeligheidslabels/bewaarregels. **Belangrijke correctie op de brief:** "Copilot = ChatGPT + Claude" is te kort door de bocht voor ons EU-publiek - Claude is een optie die voor Belgische tenants bewust aangezet moet worden en data buiten de EU brengt; die nuance staat expliciet in de post (aparte sectie "Één nuance voor Belgische bedrijven"). Geen verzonnen ROI-cijfers gebruikt, bewust kwalitatief met concrete voorbeelden gewerkt.
15. **Blogpost over gesponsorde tokens + prijsmodellen (2026-07-25):** Nieuwe post `src/content/blog/ai-tokens-niet-eeuwig-gesponsord.md` (+ SVG-cover `public/media/blog/ai-tokens-niet-eeuwig-gesponsord.svg`, motief: vlakke prijslijn die omhoog springt met een tweede pad dat vlak blijft). Kern: het abonnementsmodel dekt de echte compute-kost niet, het kantelt richting verbruiksfacturatie, en de echte verzekering is kunnen wisselen van model. **Geverifieerde ankers (juli 2026):** Claude API-lijstprijzen (Opus 5 $5/$25, Sonnet 5 intro $2/$10 t.e.m. 31 aug 2026 daarna $3/$15, Haiku 4.5 $1/$5 per MTok; cache hit 0,1x input; batch -50%) via platform.claude.com/docs/en/about-claude/pricing; Claude Pro $20/m, Max vanaf $100/m via claude.com/pricing; **Microsoft 365 Copilot $30/user/month** en **Copilot Credits $200/pack van 25.000 per maand** via microsoft.com/microsoft-365-copilot/pricing/copilot-studio (pay-as-you-go = $0,01/credit); **GitHub Copilot: alle plannen op verbruiksfacturatie sinds 1 juni 2026**, afgerekend in AI Credits van $0,01, code completions blijven inbegrepen (github.blog changelog, primaire bron); Google bundelde Gemini in Workspace en verhoogde Business Standard van $12 naar $14 (maart 2025); Epoch AI over dalende inferentieprijzen (9x tot 900x per jaar per taak, mediaan ~50x, met caveats); SemiAnalysis (juni 2026) over volledig uitgemolken abonnementen (~$14.000 compute voor ChatGPT Pro, ~$8.000 voor Claude Max) - **secundair gesourced**, daarom in de post expliciet toegeschreven aan SemiAnalysis; Anthropic's eerste operationele kwartaalwinst Q2 2026 als "naar verluidt" gebracht. **Bewuste keuzes:** het rekenvoorbeeld (200k in / 15k uit per agent-run, 10 mensen x 4 runs/dag x 20 dagen) staat met zichtbare aannames in de post in plaats van een verzonnen ROI-cijfer; alle bedragen in dollar omdat de lijstprijzen in dollar gepubliceerd worden; nuance "per token wordt het goedkoper, je factuur stijgt toch" toegevoegd zodat het geen doemverhaal wordt; open-weight-alternatieven met de waarschuwing dat licenties per model verschillen (Mistral publiceert deels Apache 2.0, deels niet - niet zomaar "open = vrij commercieel te draaien" schrijven). **Let op bij nieuwe posts:** er draaide die dag een parallelle sessie die `copilot-cowork-pay-as-you-go.md` schreef (zelfde datum, overlappend thema pay-as-you-go/Copilot Credits). Check `src/content/blog/` op recente, overlappende posts voor je een nieuw onderwerp uitwerkt.
16. **PostHog-meting gerepareerd: identiteit, hosts en dev-ruis (2026-07-25):** PostHog toonde over 24 uur exact **53 pageviews / 53 sessies / 53 unieke bezoekers** en 28 sessies met `monkai.business` of `www.monkai.business` als *initial referring domain* (self-referral). Diagnose met SQL op het project (231499) bevestigde het hard: `SELECT $session_id, count()` gaf **elke sessie precies 1 pageview, 66 op 66**. Oorzaak: `persistence: 'memory'` uit lesson #9. Dat bewaart distinct_id en session_id alleen in het JS-geheugen, en dit is een **klassieke multi-page site** (geen ClientRouter, geen View Transitions, geen prefetch), dus elke navigatie start een nieuw `window` en dus een nieuwe identiteit. Elke interne klik werd daardoor een nieuwe "bezoeker" met de vorige pagina als referrer. **Fixes:** `persistence: 'localStorage+cookie'`, `cross_subdomain_cookie: true` (cookie op `.monkai.business`, apex + www zijn dezelfde bezoeker), `ui_host: 'https://eu.posthog.com'` naast de al correcte `api_host`, en `defaults: '2026-06-25'`. **Die defaults-datum is geverifieerd in de bron, niet uit het hoofd:** de docs-pagina toont een verouderd voorbeeld (`2025-05-24`), de echte union staat in `@posthog/types` → `ConfigDefaults = '2026-06-25' | '2026-05-30' | '2026-01-30' | '2025-11-30' | '2025-05-24' | 'unset'` (opgehaald via `curl https://cdn.jsdelivr.net/npm/@posthog/types/dist/posthog-config.d.ts`). Expliciet gezette opties gaan voor op de snapshot. **Dev/preview-ruis:** er kwamen events binnen van `localhost:4321..4325` en `monkaibusiness.netlify.app`. Nu dubbel gesloten: renderen gebeurt enkel bij `import.meta.env.PROD`, en het script doet een runtime-check `location.hostname !== analyticsHost` (waarde afgeleid van `Astro.site`, dus geen tweede hardcoded domein). **www:** `www.monkai.business` serveerde de site echt (4 pageviews met dat `$host`), er was géén 301. Toegevoegd in `netlify.toml` met een volledige URL in `from` en `force = true` (nodig, anders schaduwt een bestaand bestand de regel), voor http en https apart zoals Netlify aanraadt. **Belangrijke consequentie, expliciet gemeld aan Stijn:** cross-page-identiteit kan technisch alleen met opslag op het toestel, dus de "cookieloos, geen banner nodig"-claim uit lesson #9 gaat niet meer op. `/cookies` en `/privacy` zijn daarom herschreven naar wat er echt gebeurt (één first-party `ph_…_posthog` cookie + local storage, willekeurig nummer, max 1 jaar, hoe je het weigert). Een consent-banner is bewust **niet** gebouwd - dat is een aparte beslissing die nog openstaat. **Formulier:** het is **Netlify Forms**, niet Web3Forms (dat stond fout in de brief); honeypot `bot-field` zit er al in en Netlify's spamfilter staat standaard aan. `/bedankt` kreeg 8 pageviews en er waren 8 `contact_submitted`-events, dus 1 op 1 - geen prefetch-ruis (er bestaat nergens een `<a href="/bedankt">`, en Astro-prefetch staat uit; de pagina is wél gewoon via een directe GET op te vragen omdat ze statisch is). Het event op `/bedankt` is nu wel afgeschermd tegen dubbeltellen: geen capture bij `navigation.type` `reload`/`back_forward` en alleen met een same-origin referrer. **Meet conversie op `contact_submitted`, niet op de pageview van `/bedankt`.** Een reverse proxy via een eigen `/ingest`-pad is besproken maar niet doorgevoerd (zie het gesprek: het verbergt de meting voor adblockers, maar Netlify rekent het als proxy-traffic en het breekt stil als PostHog van hostnaam verandert).
17. **Blogpost Chat/Cowork/Code + productstand juli 2026 (2026-07-25):** Nieuwe post `src/content/blog/chat-cowork-code-welke-claude-wanneer.md` (+ hand-authored SVG-cover, drie panelen: één cirkel → cirkel met satellieten → dicht raster). Opzet is een beslisgids ("wat gebruik je wanneer"), ~615 woorden. **Geverifieerde productstand (alles via web search, niet uit het geheugen):** *Claude Cowork* lanceerde jan 2026 als desktop-app, sinds **7 juli 2026** ook web + mobiel (eerst Max, andere plannen volgen); zit in Pro/Max/Team/Enterprise; draait op dezelfde agent-architectuur als Claude Code zonder terminal. **De web/desktop-splitsing is het inhoudelijke kernpunt:** enkel de desktop-app krijgt lokale bestanden, browser use, computer use en live artifacts; web/mobiel draait op Anthropic-servers, kan taken laten doorlopen terwijl je toestel uit staat, maar raakt lokale mappen alleen aan zolang de desktop-app op die machine openstaat. Bekende limieten om te vermelden: fors hoger verbruik dan chat, geen geheugen tussen sessies behalve binnen een project. *ChatGPT* heeft sinds **9 juli 2026 "ChatGPT Work"** - let op: **niet** "Cowork", het is een derde modus naast Chat en Codex, inbegrepen in Plus/Pro/Business/Enterprise. *Copilot Cowork* (GA **16 juni 2026**) heet wél letterlijk zo en draait op **Anthropic Opus 4.8 + Sonnet 4.6**; die modellen vallen **buiten de EU Data Boundary** en staan voor EU/EFTA- en UK-tenants standaard uit, dus voor Belgische KMO's is dit niet zomaar beschikbaar (consistent met lesson #14). **Overlap-waarschuwing:** de prijs-/pay-as-you-go-discussie over Copilot Cowork staat al in `copilot-cowork-pay-as-you-go.md` - die is hier bewust **niet** herhaald maar gelinkt (interne links in blogposts zijn gewoon markdown, zie ook `collective-brain-bedrijfsgeheugen.md`). **Werkwijze-les:** de blog groeide tijdens de sessie (er stonden bij de start 6 posts, bij de build 11) - lijst `src/content/blog/` opnieuw vlak vóór je schrijft, anders schrijf je onbewust een bestaande post over. **Build-ruis:** een `[glob-loader] Duplicate id`-waarschuwing net na het toevoegen van een nieuwe post is een stale content-layer-cache, geen echt duplicaat; ze verdwijnt bij de volgende build. SVG-covers visueel checken kan snel door ze met `sharp` naar PNG te rasteren in de scratchpad en die te lezen.
17. **Governance-blogpost via prompts en agents (2026-07-25):** Nieuwe post `src/content/blog/governance-zit-in-je-prompts.md` (+ SVG-cover `public/media/blog/governance-zit-in-je-prompts.svg`, motief: één donkere sjabloonkaart met een pijl naar drie steeds lichtere kopieën, alles binnen een gestippeld kader). Kern: een beleidsdocument werkt via geheugen, een gedeelde prompt of agent past de afspraak elke keer opnieuw toe - dus zet de afspraken (welke bron telt, wat er niet in mag, wat er gebeurt bij twijfel, waar een mens kijkt) in het artefact zelf. **Geverifieerd bij primaire bronnen (juli 2026):** gedeelde Projects in Claude bestaan op Team/Enterprise met "can view" vs "can edit" op de instructies, plus org-brede Organization preferences (support.claude.com); admin-provisioned **skills** gaan in één keer naar de hele organisatie en delen wordt gelogd in het auditlogboek (support.claude.com/en/articles/13119606); ChatGPT-beheerders zetten de **maximale deelscope van custom GPT's** (workspace-only vs breder); **Copilot Agent Builder-agents krijgen geen nieuwe rechten** - wie zonder agent niet bij een SharePoint-site, Teams-kanaal of mailbox kan, ziet er via de agent ook niets van - en tenantbeheerders regelen agent access/sharing/publishing in het Copilot Control System (learn.microsoft.com); **Gems worden bewaard en gedeeld via Google Drive**, dus Drive-deelinstellingen (inclusief extern delen) gelden ook voor Gems, en eerder gedeelde Gems blijven bereikbaar via Drive nadat je Gem-sharing uitzet (knowledge.workspace.google.com). **Overlapcheck was nodig en leverde een aanpassing op:** tijdens deze sessie verscheen uit een parallelle sessie `expert-collega-je-eigen-ai-agent.md`, dat al uitlegt wát Projects/Custom GPTs/Gems/declaratieve agents zijn. De tool-paragraaf is daarom herschreven zodat ze niet herhaalt wat die tools zijn, maar alleen de deel- en beheerlaag behandelt. Bevestigt de waarschuwing uit lesson #15: check `src/content/blog/` opnieuw **na** het schrijven, niet alleen ervoor. **Positionering:** de post eindigt bewust met wat je nog wél op papier zet (AI Act: zicht op tools, AI-geletterdheid, uitlegbaarheid) zodat hij naast in plaats van over `eu-ai-act-kmo.md` en `shadow-ai-verbieden-werkt-niet.md` staat. **QA-truc voor covers:** in plaats van de browser te openen, is de SVG lokaal met `sharp` naar PNG gerasterd in de scratchpad en als afbeelding teruggelezen - snelle visuele controle zonder browsersessie.
18. **Blogpost over Skills als tool-overstijgend concept (2026-07-25):** Nieuwe post `src/content/blog/skills-een-keer-vastleggen.md` (titel "Skills: leg het werk één keer vast, gebruik het overal"), tags `["agents","automatisatie","productiviteit"]` (Stijn koos thema-tags i.p.v. tool-namen, in lijn met de rest van de blog). Kern (door Stijn aangescherpt): een skill is **niet** een prompt, **niet** je prompt-bibliotheek en **niet** een custom agent/GPT - het is een afgebakend, herbruikbaar recept voor één terugkerende taak, van overal oproepbaar, telkens hetzelfde resultaat. Lopend voorbeeld: dagelijks PDF's hernoemen (AI omdat de PDF zelf varieert, een klassiek script breekt daarop). **Geverifieerd via web search (juli 2026), niet uit het geheugen:** *Claude Skills* = map met `SKILL.md` (frontmatter naam+beschrijving + instructies, optioneel scripts/references/assets); **"Record a skill"** bestaat echt maar zit in **Claude Cowork, momenteel Mac-only** (Pro/Max/Team) - op Windows beschrijf je de skill zelf. *ChatGPT Skills* gelanceerd 2026, uitdrukkelijk als **opvolger van custom GPT's** (dus skills ≠ custom GPT, ook cross-tool). *Microsoft Copilot Cowork skills* = **zelfde `SKILL.md`-mapformaat**, mapje in OneDrive; EU-caveat blijft (Cowork draait op Claude-modellen buiten de EU Data Boundary, staat voor EU-tenants standaard uit - consistent met lesson #14/#17). Skill-chaining + orchestrator + subagents is een reëel, gedocumenteerd patroon (consultancy-voorbeeld: intake-skill → plan-skill → orchestrator → subagents met eigen specialiteit). **Belangrijk onderscheid dat in de post en het cluster staat:** een [expert-collega](/blog/expert-collega-je-eigen-ai-agent) (custom agent) is een *wie*, een skill is een *hoe* - een agent kan skills gebruiken. Interne links: expert-collega, chat-cowork-code, copilot-cowork-pay-as-you-go, plan-mode, collective-brain, second-brain. **Nieuwe gotcha (belangrijk):** een **inline `<svg>` in een `.md`-blogpost rendert NIET** - Astro/remark ziet de ingesprongen SVG-regels als een indented code block en de `<text>`-inhoud lekt als platte tekst. Oplossing: maak het diagram als **los `.svg`-bestand** in `public/media/blog/` en verwijs met een enkele `<img>`-tag (`style="width:100%;height:auto"`). Zo'n via `<img>` geladen SVG erft geen webfonts, dus gebruik in de SVG een **web-veilige font-stack** (`system-ui, 'Segoe UI', Arial, sans-serif` en `ui-monospace, 'Courier New', monospace`) i.p.v. Work Sans/JetBrains Mono. Twee visuals gemaakt: cover `skills.svg` (centraal skill-blok dat van 5 kanten wordt opgeroepen) en diagram `skills-orchestrator.svg` (intake→plan→orchestrator→3 subagents). Beide visueel bevestigd in de browser (localhost preview). **Werkwijze-les herhaald (zie #15/#17):** de blog groeide tijdens de sessie van 4 naar 12 posts via parallelle sessies - lijst `src/content/blog/` opnieuw vlak vóór én na het schrijven en link naar wat er intussen bij kwam.
19. **Blogoverzicht herbouwd: één kolom, tagfilter en zoekveld (2026-07-25):** `/blog` was een 3-koloms kaartengrid; het is nu **één kolom onder elkaar** (thumbnail 260px links, tekst rechts, gestapeld onder 768px) met bovenaan een **zoekveld** en een rij **tagknoppen**. Alles client-side in een inline script op `src/pages/blog/index.astro`: elke `.post` draagt `data-tags` en een voorgerenderd, lowercase `data-search` (titel + description + tags). Meerdere tags = **OF**, de zoekterm werkt daar bovenop als **EN**. De bodytekst zit bewust **niet** in de index - dat zou ~50 KB extra HTML op de listing zetten voor 12 posts; als volledige full-text zoek later toch nodig is, is dat de afweging om opnieuw te maken. Actieve filters staan in de querystring (`/blog?tags=governance,kennis&q=copilot`) via `history.replaceState` en worden bij het laden weer toegepast, dus zulke links zijn deelbaar. **Tagvocabulaire opgeschoond (Stijn stuurde de set zelf bij):** de 12 posts hadden samen 17 tags met dubbels op hoofdletters (`claude`/`Claude`, `copilot`/`Copilot`) en overlappende betekenis (`adoptie`/`ai-adoptie`/`kmo`, `collective brain`, `microsoft`, `strategie`, `prompts`, `skills`). Eindstand: **10 vaste tags**, zes thema's (`ai-adoptie`, `governance`, `productiviteit`, `kennis`, `agents`, `automatisatie`) en vier tools (`claude`, `chatgpt`, `copilot`, `gemini`), altijd kleine letters. Stijns keuzes daarbij, zodat je ze niet terugdraait: **`adoptie` heet `ai-adoptie`**, **`wetgeving` is opgegaan in `governance`** (de AI-Act-post draagt dus geen aparte wetgevingstag meer), **`kosten` is geschrapt** (de twee prijsposts staan nu op `ai-adoptie` + `governance` resp. `copilot`), en **`chatgpt` + `gemini` zijn toegevoegd** naast de al bestaande tooltags. Tooltags zijn toegekend op basis van wat er echt in de post staat, niet op losse vermeldingen: `gemini` haalt daardoor maar 2 posts (expert-collega en governance-in-je-prompts, de enige twee met een echte Gems-passage). Elke tag is een knop, dus een nieuwe tag toevoegen = een extra knop; zie de uitgebreide regel bij "How to add a blog post". Merknamen krijgen hun schrijfwijze uit een `labels`-map in `blog/index.astro`, want de default zou "Chatgpt" en "Ai-adoptie" tonen. De knoppen worden bij het builden uit de frontmatter afgeleid en gesorteerd op aantal posts, met het aantal als superscript. **Gotcha opnieuw bevestigd (zie #11):** het `hidden`-attribuut als toggle werkt alleen met een expliciete `[hidden]{display:none !important}` in de component-CSS, want `.post` heeft `display:grid`. Zelfde regel geldt voor de `.posts`-container, die bij 0 resultaten óók verborgen wordt zodat de lege staat niet onder een gat met witruimte hangt. **Niet aangeraakt:** `BlogTeaser.astro` op de homepage blijft een 3-koloms grid met de nieuwste 3 posts, en de detailpagina toont (nog) geen tags. **Browser-QA-nota:** `resize_window` van de Chrome-tools kreeg het venster deze sessie niet versmald (screenshot bleef 1522px), dus de mobiele variant is niet visueel bevestigd - de responsive regels volgen wel exact het patroon van de rest van de site en beide flexrijen (`.chips`, `.meta`) staan op `flex-wrap: wrap`, zodat de overflow-valkuil uit #7 niet speelt.
20. **Consent-banner voor de meting (2026-07-25):** Na lesson #16 bewaart de meting een herkenningsnummer op het toestel, en daarmee valt ze onder de toestemmingsplicht. **Geverifieerd bij de bron, niet uit het hoofd:** de GBA-FAQ antwoordt op de vraag of publieksmetingcookies toestemming nodig hebben letterlijk "Ja", met als motivering "In de huidige wetgeving is er geen vrijstelling van de verplichting om de toestemming van de betrokkenen te verkrijgen"; dat geldt ook voor first-party en voor geaggregeerde statistiek, en België heeft de lossere lijn van Frankrijk en Nederland **niet** overgenomen. De ePrivacy-verordening is ingetrokken op 11 februari 2025; de Digital Omnibus (19 november 2025) zou via een nieuw GDPR-artikel 88a een vrijstelling voor geaggregeerde publieksmeting invoeren, maar de **cookiebepalingen zijn in juni 2026 uit de compromistekst van de Raad geschrapt** en het dossier lag begin juli 2026 nog open. Reken dus niet op die vrijstelling, en let op dat ze zelfs bij goedkeuring niet automatisch geldt: ze eist *geaggregeerde* informatie, terwijl PostHog een individuele gebeurtenissenstroom bewaart. **Bouw:** nieuwe `src/components/ConsentBanner.astro` (kaartje links onderaan, keuze van de gebruiker) + `Analytics.astro` gestript tot alleen `window.__monkaiAnalytics.start()`. De banner beslist, Analytics weet enkel hoe je PostHog start; die grens strikt houden. Zonder toestemming wordt er niets geladen: geen script, geen netwerkoproep, geen cookie. Keuze in `localStorage` onder `monkai_consent` (`{v,choice,ts}`), weigering verloopt na zes maanden, toestemming niet. DNT aan = behandeld als weigering, banner wordt dan niet getoond. Herroepen via één knop op `/cookies`. Grondslag op `/privacy` verschoven van gerechtvaardigd belang naar **toestemming**. **Twee echte bugs die alleen uit browsertests kwamen, niet uit een groene build:** (1) `posthog.reset(true)` **wist niets** - het genereert een nieuwe id en schrijft die terug, dus na het intrekken van toestemming stond er een vers identificatienummer op het toestel. Wis de opslag met de hand. (2) PostHog gebruikt **twee** voorvoegsels: `ph_...` voor identiteit en flags, en **`__ph_opt_in_out_<token>`** voor de opt-out-vlag. Die tweede vergeten is stil dodelijk: `opt_out_capturing()` zet de vlag, en wie ze laat staan krijgt een site die na een nieuwe "Accepteren" nog steeds `has_opted_out_capturing() === true` heeft en dus zwijgend niets meet. De wisfunctie dekt nu beide voorvoegsels, en cookies worden op alle domeinvarianten verwijderd (host én `.monkai.business`, want `cross_subdomain_cookie` zet ze op het hoofddomein en een cookie verdwijnt alleen bij een exacte domain-match). **Testtruc zonder de source te wijzigen:** de hostcheck blokkeert meten buiten `monkai.business`, dus voor een lokale end-to-end-test `npm run build` en daarna in het (gitignored) `dist/` met sed `analyticsHost = "monkai.business"` naar `"localhost"` patchen, dan `npm run preview`. Nooit `astro.config.mjs` aanraken. Let op: die test stuurt echte events naar project 231499 met `$host = localhost:<port>`, dus filter die eruit. **Ook geleerd:** gesimuleerde muisklikken vlak na een navigatie komen soms niet aan; `element.click()` via de JS-tool is betrouwbaarder om logica te testen, en `resize_window` veranderde de viewport hier niet, dus de 390px-weergave is door constructie afgedekt (`min(360px, calc(100vw - 40px))` plus de 768px-override) en niet visueel bevestigd.
21. **LinkedIn-profiel toegevoegd (2026-07-25):** `https://www.linkedin.com/in/stijndeketelaere/` staat nu op drie plaatsen: als link "Volg mij op LinkedIn" onderaan de `About`-sectie, als "Stijn op LinkedIn" in de derde footerkolom (bij mail/adres), en als `sameAs` op de `Person`-node in de sitewide JSON-LD van `BaseLayout.astro` - dat laatste is het punt dat telt voor Google en AI-engines, want het knoopt de persoon op de site vast aan het profiel. Ook toegevoegd aan het contactblok in `public/llms.txt`. Externe links krijgen `target="_blank" rel="noopener"`. Bij het aanraken van deze bestanden zijn de resterende em-dashes vervangen door een gewone hyphen (About-tekst, llms.txt-intro), conform de huisregel.
22. **Loopbaan toegevoegd aan "Over Stijn" (2026-07-25):** De About-sectie was twee alinea's; er staan nu vier extra alinea's tussen de intro en de slotalinea ("Ik geloof niet in technologie omdat het kan"), chronologisch: VSK Electronics (technische start, mens-machine-interactie, nog altijd Raspberry Pi en IoT) → S&V Management Consultants, later onderdeel van PwC (Business Cockpit, data + business) → Proceedix, later onderdeel van SymphonyAI (technical lead, daarna customer success lead; AI in de praktijk: anomaliedetectie, preventief onderhoud, state tracking; vanaf 2022 GenAI) → zelfstandig sinds 2025. **Schrijfwijzen geverifieerd via web search, niet uit het geheugen:** Proceedix (Gent) werd in december 2021 overgenomen door **Symphony Industrial AI**, onderdeel van **SymphonyAI** (dus die hoofdletters, niet "SymphonyAi"); PwC België nam S&V Management Consultants over. "Business Cockpit" komt niet in publieke bronnen voor - dat is Stijns eigen projectnaam en is zo overgenomen. In de markup staat `S&amp;V`. Verder niets aan de layout gewijzigd: de foto blijft `align-items:center`, wat met de langere tekstkolom nog steeds oogt. **Nota:** `npm run build` naast een lopende `npm run dev` gaf op de dev-server een `UnknownFilesystemError` uit de content-layer-cache; herstart de dev-server na een build in plaats van het als een echte fout te lezen (verwant aan de stale-cache-waarschuwing uit lesson #17).
23. **Blogpost over accelerated coding (2026-07-25):** Nieuwe post `src/content/blog/accelerated-coding-geen-vibe-coding.md` (+ cover `public/media/blog/accelerated-coding.svg`, twee kaarten: links donkere code met een gesloten oog, rechts dezelfde code met snelheidsstrepen en een vinkje), tags `["productiviteit","ai-adoptie"]`. Kernstelling van Stijn: **accelerated coding ≠ vibe coding**. Vibe coding is code laten schrijven zonder er nog naar te kijken; accelerated coding is hetzelfde werk dat je anders zelf schreef, veel sneller en met minder fouten, mét review. Boodschap richting developers: juist met een technische achtergrond haal je hier het meeste uit, en de winst zit in de dagelijkse kleine frustraties (boilerplate, testdata, regex, config, migratiescripts, stack traces, een onbekende codebase), niet in "bouw mijn applicatie". **Alles geverifieerd via web search, niet uit het geheugen:** Karpathy muntte "vibe coding" in februari 2025 ("forget that the code even exists", "I don't read the diffs anymore", expliciet bedoeld voor weggooiprojecten van een weekend); **Collins woord van het jaar op 6 november 2025**, met de neutrale definitie "the use of artificial intelligence prompted by natural language to write computer code"; **Stack Overflow Developer Survey 2025**: 84% gebruikt AI of is het van plan, 46% wantrouwt de juistheid tegenover 33% die ze vertrouwt, grootste frustratie "almost right, but not quite" bij 66%, en 45% zegt dat debuggen van AI-code meer tijd kost; **METR (juli 2025)**: 16 ervaren open-source developers, 246 echte taken, 19% trager mét AI terwijl ze vooraf 24% winst verwachtten en achteraf nog 20% winst dachten te zien - in de post staat de caveat expliciet (tools van begin 2025, Cursor Pro met Claude 3.5/3.7 Sonnet, onderzoekers waarschuwen zelf tegen doortrekken); **DORA 2025** framet AI als "amplifier" van bestaande sterktes en zwaktes; **Simon Willison** ("Not all AI-assisted programming is vibe coding", maart 2025) levert de bruikbare vuistregel: commit niets wat je niet aan iemand anders kan uitleggen. Interne links naar plan-mode en skills. Cover visueel gecontroleerd door hem met `sharp` naar PNG te rasteren in de scratchpad (zie #17/#18), geen browsersessie nodig.
24. **Verborgen /inspiratie-pagina met kopieerbaar materiaal (2026-07-26):** Nieuwe collectie `inspiratie` + pagina `src/pages/inspiratie/index.astro` waar deelnemers aan een inspiratiesessie instructies, expert-collega's, prompts, skills en checklists ophalen. Zie de sectie "De /inspiratie pagina" hierboven voor de vorm. **Keuzes die Stijn maakte of goedkeurde:** geen inlogdrempel (in tegenstelling tot `/data`), want ze moeten meteen kunnen ophalen; splitsing per systeem én per soort materiaal; en de `agents`-categorie heet in de UI **"Expert-collega's"**, in lijn met de blogpost `expert-collega-je-eigen-ai-agent.md`. Zijn eigen Claude-instructieset staat er verbatim op als voorbeeld (`claude-instructies-stijn.md`) - **let op: die noemt PinPoint, Odum en Outlook**, dus het is klantcontext op een pagina die publiek is voor wie de URL heeft. Bij twijfel: genericiseren. **Ontwerpbeslissing die je niet moet terugdraaien:** `system` mag een **lijst** zijn (`z.union([enum, array(enum)])`), zodat één bestand onder meerdere tabs verschijnt; de vier expert-collega's (SEO, HR, kritische tegenlezer, moppentapper) staan zo één keer op de schijf in plaats van vier keer gekopieerd, en de platformspecifieke uitleg ("waar zet je dit in Claude/ChatGPT/Copilot/Gemini") zit als één `note`-regel per tab in de pagina. **Technisch punt:** de body gaat onbewerkt via `entry.body` in een `<pre>`, dus markdown blijft letterlijk staan - dat is nodig omdat de klant de koppen en opsommingen mee moet kunnen kopiëren. Kopieerknop gebruikt `navigator.clipboard` met een `textarea`+`execCommand`-fallback; **clipboard werkt enkel in een secure context** (https of localhost), dus test op de preview en niet via een file-URL. Ook hier weer de `[hidden]{display:none !important}`-regel meegenomen (les 11/19). **Browsertest gedaan:** tabwissel, querystring (`?systeem=gemini`), openklappen en kopiëren bevestigd op `npm run preview`; `navigator.clipboard.readText()` faalt in de test met "Document is not focused", dus controleer het knoplabel ("Gekopieerd") in plaats van de klembordinhoud. Sitemapfilter in `astro.config.mjs` uitgebreid; `robots.txt` bewust **niet** aangepast, want een `Disallow: /inspiratie` zou het pad juist publiek maken.
25. **Blogpost over prompt engineering in 2026 (2026-07-26):** Nieuwe post `src/content/blog/prompt-engineering-2026-uitkomst.md` (+ cover `public/media/blog/prompt-engineering-2026.svg`: opdrachtkaart, een wedervraag die eerst terugkomt, dan een pijl naar een doelwit), tags `["productiviteit","ai-adoptie"]`, ~690 woorden. Stijns stelling: prompt engineering is minder belangrijk geworden, de uitkomst is wat telt; hij gebruikt zelf nog "wie, wat, hoe" maar met het gewenste resultaat als zwaarste blok, en zet altijd "stel mij vragen als je niet zeker bent" onderaan. **Geverifieerd bij primaire bronnen, niet uit het geheugen:** OpenAI's reasoning-richtlijnen (developers.openai.com, `/docs/guides/reasoning-best-practices` - let op, `platform.openai.com` 301't daarheen) zeggen letterlijk "keep prompts simple and direct", dat "think step by step" onnodig is omdat het model intern al redeneert, "try zero shot first", en dat je heel specifiek moet zijn over je einddoel met de parameters van een geslaagd antwoord. Anthropics prompting-richtlijnen (platform.claude.com, `/docs/en/build-with-claude/prompt-engineering/...` redirect naar één grote "Prompting best practices"-pagina): "Being specific about your desired output can help", de **golden rule** (laat je prompt lezen door een collega zonder context; snapt die het niet, dan snapt Claude het ook niet), context/motivatie meegeven helpt, "provide clear success criteria", en - het scherpste anker - **bij de nieuwste modellen moet je aggressieve taal juist afzwakken**: waar je vroeger "CRITICAL: You MUST use this tool when..." schreef, volstaat nu "Use this tool when...", anders overtriggeren ze. Diezelfde pagina bevat voorbeeldinstructies om een model terughoudend te maken bij een dubbelzinnige vraag ("default to providing information, doing research, and providing recommendations rather than taking action"), wat het gedocumenteerde mechanisme is achter Stijns "vraag het me"-regel. De naamsverschuiving naar **context engineering** komt van Tobi Lütke (Shopify, 19 juni 2025) en Andrej Karpathy, met Simon Willisons uitleg waarom dat ertoe deed; gesourced via simonwillison.net omdat x.com niet betrouwbaar te fetchen is. **Bewust niet gebruikt:** de rondgaande cijfers over dalende "prompt engineer"-vacatures (-30%, -40%, "78% van de AI-vacatures"). Die komen enkel uit SEO-blogs zonder primaire bron; niet opnemen tenzij je het bij Indeed Hiring Lab of LinkedIn zelf vindt. **Overlap bewaakt:** de post herhaalt plan mode, skills en expert-collega niet maar linkt ernaar. Cover visueel gecontroleerd via `sharp` naar PNG in de scratchpad; `extract()` op een detail is handig om een pijlpunt te beoordelen.
